# Check that ifdef / else work
@ifdef TEST
    LITA 12
@else
    LITA 0
@endif

# Check that ifndef works
@ifndef TEST
    LITA 0
@else
    LITA 12
@endif

# Check that if/elif work
@ifdef TEST
    @if TEST == 1
        LITB 1
    @elif TEST == 2
        LITB 2
    @endif
@endif
